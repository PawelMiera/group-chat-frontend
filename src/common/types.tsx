
export interface ChatMessageInterface {
    message: string;
    user: string;
    time: string;
}

export interface GroupInterface {
    name: string;
    last_message: string;
    last_user: string;
}